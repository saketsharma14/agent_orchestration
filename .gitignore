# Python
__pycache__/
*.py[cod]
*.pyo

# Virtual environments
venv/
.venv/
env/
agent_env/

# VS Code
.vscode/

# macOS
.DS_Store

# Logs
*.log

# Environment variables
.env

# Jupyter
.ipynb_checkpoints/

# --- Generated search/ablation artifacts ---
# These are regenerated by scripts/ every run and aren't meaningful in
# source control (they change on every re-run, and there can be dozens of
# them per experiment). Regenerate via the commands in README.md.
archive_log.json
archive_ablation_*.json
archive_agentabl_*.json
archive_dist_run*.json
seed_lr_sweep_log.json

# Ad-hoc console redirects
*_out.txt

# Per-run CSVs written directly to repo root by the scripts. The specific
# ones backing report numbers are copied into results/ instead (NOT
# ignored -- see .gitignore exception below / README "Reproducing" section).
/ablation_structural_vs_numeric_results.csv
/ablation_agent_mode_results.csv
/distribution_results.csv
/seed_lr_sweep_results.json

# Standard Python/data noise
__pycache__/
*.pyc
data/
.ipynb_checkpoints/